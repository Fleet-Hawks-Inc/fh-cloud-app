import { NavOpenedDirective } from './nav-opened.directive';

describe('NavOpenedDirective', () => {
  it('should create an instance', () => {
    const directive = new NavOpenedDirective();
    expect(directive).toBeTruthy();
  });
});
